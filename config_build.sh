#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=thunderbird-ensemble
CHROME_PROVIDERS="content"
CLEAN_UP=1
ROOT_FILES=
ROOT_DIRS="modules tests"
BEFORE_BUILD=
BEFORE_PACK=
AFTER_BUILD=
